node_modules
dist
.env
.vscode

# Next.js build output
.next
.vercel

# Vite build output
dist

# Environment
.env.local
.env.*.local

# Logs
logs
*.log
npm-debug.log*
Readme/supa.txt

# INSIGHTWOS V6 — Bootstrap (zero-trace)
scripts/bootstrap-admin.ts
scripts/cleanup-bootstrap.sh
.env.local
.env.local.example

# Duplicate/backup directories
WOS-Web-fresh/

# Audit and test artifacts (should not be committed)
.freebuff/

# Artefak lokal: arsip GAS mentah, forensik, scratch, arsip .rar — never commit
supabase/GAS sebelum refaktor/
supabase/scripts/forensic_audit.py
forensic_report.md
WOS-Web.rar
tmperr/
.clai/
test-results/
playwright-report/
provision-results.csv
_env.local.backup

# Test scratch logs (diag/sweep/test runs) — nooit committen
*.txt

# Supabase smoke files (contain secrets)
supabase/smoke/

# Plaintext credential sheets — NEVER commit (bagikan per-orang via WA lalu hapus)
supabase/akun/

# Local scratch / one-off scripts / backups — nooit committen
*.bak
deploy_verification.py
Readme/*.txt

# ⚠️ REAL PII encryption key (plaintext) — NEVER commit.
# File seeding encryptie-key naar company_config; rotate key & delete file
# (zie docs/5.0-credential-rotation-runbook.md).
supabase/seed_company_config.sql

# One-off DB fixes (plaintext passwords) � applied & verified, never commit
supabase/scripts/fix-nrp003.sql
.temp_files/

.wrongstack/
!/.wrongstack/
/.wrongstack/*
!/.wrongstack/project.json
.env*

# Legacy local archive (GAS source, forensik, lampiran audit) — arsip lokal, jangan commit
Readme/
files/
# ── Agen workspace & scratch (aturan §0.11 AGENTS.md: dilarang menaruh
#    skrip/log/laporan sementara di root) ── never commit
.agents/
$null
temp-*.mjs
verify_*.py
.vercel/
dist/
